"""
PDF parsing for DocMind AI (future phases).

Phase 1 only handles file upload and storage.
Text extraction and document understanding will be added here later.
"""


def placeholder_parse_pdf(file_path: str) -> None:
    """
    Reserved for future use: read a PDF from disk and extract text or structure.

    Args:
        file_path: Absolute or relative path to a saved PDF file.
    """
    raise NotImplementedError("Parsing is not part of Phase 1 (upload only).")
